# =============================================================================
# vla_physics_v2 — Pinned for A100 driver 570.x (CUDA 12.8 max)
# =============================================================================
# Torch 2.4.1+cu124 is the highest stable torch the A100 driver supports.
# All other packages pinned to versions known to work with torch 2.4.x.
# DO NOT bump versions without testing — torch 2.5+/transformers 5.x will
# break the existing Week B model loaders (Phi3V, Pixtral, LLaVA-OV).
# =============================================================================

# Core: torch + CUDA-coupled libs (installed separately via cu124 index)
# torch==2.4.1+cu124
# torchvision==0.19.1+cu124
# torchaudio==2.4.1+cu124
# bitsandbytes==0.44.1

# HuggingFace stack — pinned to torch 2.4 era
transformers>=4.46.0,<5.0
accelerate>=0.34.0,<1.1
peft>=0.13.0,<0.14
datasets>=2.21.0,<3.0
huggingface-hub>=0.25.0,<0.27
safetensors>=0.4.5,<0.5
tokenizers>=0.20.0,<0.21

# Vision-language model deps
qwen-vl-utils>=0.0.8
pillow>=10.4.0,<11.0
einops>=0.8.0
timm>=1.0.9
sentencepiece>=0.2.0
protobuf>=4.25.0,<6.0

# Scientific computing — numpy<2 for compat with torch 2.4 + sklearn
numpy>=1.26.0,<2.0
scipy>=1.13.0,<1.15
scikit-learn>=1.4.0,<1.6
pandas>=2.2.0,<2.3
h5py>=3.11.0

# Visualization
matplotlib>=3.8.0,<3.10
seaborn>=0.13.0
plotly>=5.20.0

# Config / experiment management
hydra-core>=1.3.2
omegaconf>=2.3.0
wandb>=0.17.0,<0.19

# Utilities
tqdm>=4.66.0
rich>=13.7.0
requests>=2.32.0
pyyaml>=6.0

# Notebook / dev tooling
jupyter>=1.0.0
ipykernel>=6.29.0
ipywidgets>=8.1.0
pytest>=8.1.0
pytest-cov>=5.0.0

# Tensorboard for logging
tensorboard>=2.15.0,<2.18

# Image processing for vision models
opencv-python-headless>=4.10.0
decord>=0.6.0
