# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.Python
env/
venv/
.venv/
ENV/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data (large files — store separately; /data anchors to repo root, not src/data)
/data/
*.pkl
*.hdf5
*.h5
*.lmdb

# Model weights
*.pt
*.pth
*.ckpt
*.safetensors
*.bin

# Results (generated, not versioned)
results/saliency_maps/
results/probe_metrics/
results/ablation_metrics/
results/figures/

# Experiment tracking
wandb/
mlruns/
lightning_logs/
runs/

# Environment / secrets
.env
.env.local
*.key

# IDE
.vscode/
.idea/
*.swp
*.swo
*.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
.md
htmlcov/

# Hydra outputs
outputs/
multirun/
.hydra/

# HuggingFace cache (don't commit model caches)
.cache/

# SLURM logs
slurm-*.out
*.sbatch.log
results/*/activations/

# Local caches + logs (feature extraction, run logs)
cache/
logs/
# Week 1 raw audit log (regenerable; keep aggregate JSON instead)
results/week1/feature_extraction.jsonl
Miniconda3-latest-Linux-x86_64.sh

# Claude Code session state (local only — never commit)
.claude/

# Local-only archive of pre-submission planning/scratch markdowns
.archive/

jobs/*.out

# === Reviewer-anonymity hygiene: keep these files LOCAL ONLY ===
# Project memory / agent state
CLAUDE.md
.turing_cuda_module

# Paper drafts (the PDF goes to OpenReview; markdown drafts stay local)
paper/
docs/

# Proposal / formatting / template assets — not part of the code release
Neurips/
PhysicalAI_Proposal.docx
*.docx
Proposal Template.pdf
Where Does Physics Live in Vision Encoders.pdf
upload_phys_lens_bundle.zip

# Stale SLURM backups + ad-hoc test logs
turing/.backup_pre_v2/
physbench_test_output.log
